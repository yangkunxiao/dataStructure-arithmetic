/**
 * 插入排序：从已知数组中选取一个元素做为基准，和其他元素进行比较，然后将这个元素插入到一排序的数组中去
 * 
*/
var arr = [1, 4, 5, 2, 3, 9, 0, 7, 6];
var temp;
for (let i = 0, len = arr.length; i < len; i++) {
    for (let j = 0; j < lenSort - 1; j++) { 
        if (arr[i] >= sortList[j] && arr[i] <= sortList[j + 1]) {
            
        }

    }

}

console.log(sortList);
